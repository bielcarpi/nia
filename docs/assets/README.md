# Product media

The two screenshots in this directory came from a working local demo build:

- `product-session.png` shows an active Spanish practice session;
- `product-feedback.png` shows the review produced when that session ends.

They contain scripted demo text and no personal account information. When the
UI changes, refresh both images from `make dev-mobile` and walk through the
same session, feedback, history, and deletion flow before updating the README.
